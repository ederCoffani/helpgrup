<%= form_with(model: grup) do |form| %>
  <% if grup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grup.errors.count, "error") %> prohibited this grup from being saved:</h2>

      <ul>
        <% grup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :links %>
    <%= form.text_field :links %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
